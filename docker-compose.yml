version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./logs:/app/logs
      - ./reports:/app/reports
    restart: always
    environment:
      - FLASK_ENV=production
